node_modules/
dist/
coverage/
.vscode/y*.deb.env

# Local environment files are never committed. Keep only the documented
# non-secret example file in source control.
.env*
!.env.example

server/.env
server/.env.*
