// src/assets/components/TopPreviewBar.jsx
import React from "react";

/**
 * TopPreviewBar removed (premium clean UX).
 * Keeping the component so imports/usages don't break.
 */
export default function TopPreviewBar() {
  return null;
}